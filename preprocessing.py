
# Preprocess JSON QEP, remove unnecessary brackets
def preprocess_json(qep):
    qep = qep[0]
    qep = qep[0]
    qep = qep[0]
    print('Pre-processed QEP: ')
    print(qep)
    print()

    return qep
